class AntigravityProfile {
  // Intervals defined in Antigravity profile, not hardcoded
  static const Duration gpsUpdateInterval = Duration(seconds: 5);
  static const Duration searchTimeout = Duration(seconds: 60);
  static const double minDistanceFilter = 10.0; // meters
}
